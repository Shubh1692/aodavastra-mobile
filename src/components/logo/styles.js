import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    topContainer: {
        // borderColor: 'green',
        // borderWidth: 2,
        paddingVertical: 40,
        alignItems: 'center'
    }
});

export default styles;